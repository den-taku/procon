#![allow(dead_code)]

pub mod hungarian_library {

    #[cfg(test)]
    mod tests_hungarian {
        use super::*;

        #[test]
        fn for_hungarian() {
            assert_eq!(1 + 2, 3);
        }
    }
}
